'use client';

import { Box } from '@/app/components/Generals';

const Error = () => {
    return (
        <Box className='h-full flex items-center justify-center'>
            <div className='text-nenutral-400'>Đã có lỗi xảy ra.</div>
        </Box>
    );
};

export default Error;
